<h2> Add a good time </h2>

<%= form_for([current_user, @user.memories.build]) do |f| %>
  <p>
    <%= f.label :narrative %><br>

    <%= f.text_area :narrative %><br>
  </p>
  <p>
    <%= f.submit %>
  </p>
  <% end %>
